{
  "name": "fleet-backend",
  "version": "1.0.0",
  "description": "Fleet management backend: MQTT ingestion, fleet state, REST + WebSocket APIs, MongoDB history",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "express": "^4.21.2",
    "mongodb": "^6.9.0",
    "mqtt": "^5.10.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
